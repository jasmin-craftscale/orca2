{
  "priority": 1,
  "request": {
    "method": "POST",
    "urlPath": "/api/v1/commands",
    "bodyPatterns": [ { "matchesJsonPath": "$[?(@.action == 'SET_IO')]" } ]
  },
  "response": {
    "status": 503,
    "headers": { "Content-Type": "application/json" },
    "jsonBody": {
      "status": "ERROR",
      "detail": "STUB. A host that is there and says no — FAILED, which is knowable, quite unlike a silence."
    }
  }
}
