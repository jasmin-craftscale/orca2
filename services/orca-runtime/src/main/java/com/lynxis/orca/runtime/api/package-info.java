/**
 * orca-runtime's service-level API surface.
 *
 * <p>This is <em>not</em> a sixth runtime module. {@code ModuleWallRule} recognises
 * exactly execution, workitem, integration, notify and readmodel. This package is
 * instead the service's outer edge: generated interfaces and routes, currently
 * health, that belong to the process as a whole rather than one internal module.
 */
package com.lynxis.orca.runtime.api;
