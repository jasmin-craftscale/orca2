/**
 * The two-way door to customer systems. Owns connector configuration, event
 * dispatch and the partner API. It reaches {@code execution} only through
 * admission events in and connector invocations out.
 */
package com.lynxis.orca.runtime.integration;
