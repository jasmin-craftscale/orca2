/**
 * Repositories. Queries are constructed through the scope seam in
 * {@code platform/scope} and nowhere else — a query built outside it fails the build.
 */
package com.lynxis.orca.sync.persistence;
