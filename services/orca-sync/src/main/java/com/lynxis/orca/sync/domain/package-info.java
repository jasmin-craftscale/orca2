/**
 * Entities and domain services. Nothing here reaches into another module's
 * {@code persistence} package; the module wall is a build failure, not a convention.
 */
package com.lynxis.orca.sync.domain;
